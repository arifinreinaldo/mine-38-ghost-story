# Supabase (optional — the free sample runs without these).
# Set both to enable registration + the gated next case.
# Find them in Supabase: Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
